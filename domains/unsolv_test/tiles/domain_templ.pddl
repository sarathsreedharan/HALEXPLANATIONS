(define (domain strips-sliding-tile)
  (:requirements :strips :typing)
  (:types obj - object)
  (:predicates
   (tile ?x) (xposition ?x) (yposition ?x)
   (at ?t ?x ?y) (blank ?x ?y)
   (inc ?p ?pp) (dec ?p ?pp)
   {}
   )


    {}

  )
